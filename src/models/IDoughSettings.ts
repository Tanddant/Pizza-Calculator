export interface IDoughSettingsString {
    water: string;
    salt: string;
    yeast: string;
    weight: string;
    count: number;
}
export interface IDoughSettings {
    water: number;
    salt: number;
    yeast: number;
    weight: number;
    count: number;
}